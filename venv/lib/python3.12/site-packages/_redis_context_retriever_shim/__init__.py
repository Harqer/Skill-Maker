"""Internal marker module for the ``context-surfaces`` compatibility shim.

This module exists only so the distribution has at least one file to package.
The shim's purpose is its ``Requires-Dist: redis-context-retriever`` metadata,
which pulls in the real package providing the ``context_surfaces`` module.

Do not import this module. Use ``from context_surfaces import ...`` (provided
by the ``redis-context-retriever`` distribution) instead.
"""
